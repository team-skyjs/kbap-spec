---
name: todo
description: >
  K-Bap 할일 관리. 사용자가 "뭐 해야하지 / 뭐하고있었지 / 다음 할 일 / 이어서 하자 / 어디까지 했지"
  같은 말을 하면 발동 — 개별 이슈 상태는 Jira(KB)에서, 출시 큰그림(크리티컬패스·리스크)은 LAUNCH.md에서
  가져와 종합 보고한다. 할 일 추가/시작/완료는 Jira 전환(jira 스킬)으로 처리한다.
---

# K-Bap 할일(TODO) 워크플로

## 소스 분리 (2026-07-06 확정)
- **개별 이슈 상태의 정본 = Jira(KB)** — 진행 중/검토 중/할 일/완료, 담당, 에픽, 스프린트.
  - 조회·전환은 **`jira` 스킬** 규격 사용(사이트/cloudId/전환 id 등 거기 정의).
- **출시 큰그림 = LAUNCH.md** (`specs/001-personalized-menu-mvp/LAUNCH.md`) — 타임라인·크리티컬패스·리스크·정책 링크·회의 결정·에픽 맵. **per-이슈 체크박스 없음(중복 금지).**
- 보조: 메모리 `MEMORY.md`.

## 타임스탬프 규칙 (중요)
시각은 **절대 추측하지 말고** Bash로 실제 시각을 받아 쓴다: `date '+%Y-%m-%d %H:%M %Z'`. Jira 댓글·LAUNCH 갱신 모두 이 값 사용.

---

## 트리거 A — "뭐 해야하지 / 뭐하고있었지 / 다음 / 이어서 / 어디까지"
1. **Jira 조회**: 현재 스프린트의 `진행 중` → `검토 중` → `할 일` 이슈 (jira 스킬). 응답이 크면 fields 최소화 + jq 추출.
2. **LAUNCH.md Read**: 크리티컬 패스·진행 스냅샷·리스크로 맥락 파악.
3. **메모리 스캔**.
4. 종합 보고:
   - **🔄 진행 중 / 🔎 검토 중:** Jira 기준 (무엇을, 막힌 것 있으면)
   - **🔜 다음 큐:** 크리티컬 패스 상 우선순위 순 (의존 막힘 여부 명시)
   - **🎨 디자인/BE 대기:** 롱폴·블록 항목
   - **✅ 최근 완료:** 맥락용 1~2개
5. "이거부터 할까?" 제안.

## 트리거 B/C/D — 할 일 추가 / 시작 / 완료
**Jira에서 처리한다 (LAUNCH 체크박스 아님).** `jira` 스킬 규격 준수:
- **추가**: 새 이슈 생성 — Background+DoD·에픽·라벨·스프린트 (jira §1~4). 쓰기 전 승인.
- **시작**: 해당 이슈 → `진행 중`(전환 21).
- **완료**: **완료 게이트**(jira §5) — 구현 세션은 `검토 중`(31)까지만, `완료`(41)는 리뷰 후 예진+Codex. 완료 시 §7 BE-플래그 검토.
- 진행/블록/하루마감 시 §6 진행로그 댓글.

## 트리거 E — LAUNCH.md 갱신 (큰그림이 바뀔 때만)
per-이슈 상태가 아니라 **크리티컬 패스·리스크·타임라인·회의결정·에픽 맵·정책링크**가 바뀔 때 갱신:
1. `date` 실행.
2. 해당 섹션 갱신 + 상단 타임라인/역할 배너 최신 유지.
3. 이슈 하나 끝났다고 LAUNCH를 건드리지 않는다 — 그건 Jira 몫.

---

## 커밋/동기화
- LAUNCH.md·스킬 변경 후 의미 단위 커밋(문서 레포 관례). 커밋 메시지 끝에 세션 규칙 co-author/Codex-Session 라인 유지.
- **push는 사용자가 원할 때.**

## 원칙
- **이슈 상태 = Jira 단일 정본.** LAUNCH·메모리에 이슈 상태를 복제하지 않는다.
- 완료는 자기인증 금지 — 검토 게이트 통과 후 리뷰어가 닫는다(안전 크리티컬).
- 타임스탬프는 항상 실제 `date` 출력.
